interface:
  display_name: "Jira Window"
  short_description: "Repo-specific patterns and workflows for jira-window"
  default_prompt: "Use the jira-window repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true